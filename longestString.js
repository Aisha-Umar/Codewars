function longestString(s){
    //a string with repeating chars
    //the longest string you can get without repeating chars
    //for example abcabcbb, longest string would be abc
    //loop through the array, if you see a duplicate, drop chars 
    //from the beginning of the current str till you have all unique
    //chars
    //store the longest string in a variable
    let start=0
    let maxLength
    let chars = new Set()
    //loop through the string, keep track of chars by storing each one in
    // a Set, check if the next char is a repeating char
    //by checking in the Set and keep adding to the Set if it is
    // expand end pointer to the right
    // end++
    //else if char is repeating, check in the Set
    //doing start++
    // then end++
   //for loop with end = 0

   

}